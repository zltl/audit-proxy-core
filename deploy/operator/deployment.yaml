apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-proxy-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: audit-proxy-operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: audit-proxy-operator
    spec:
      serviceAccountName: audit-proxy-operator
      containers:
        - name: operator
          image: ghcr.io/audit-proxy-core/audit-proxy-core-operator:latest
          imagePullPolicy: IfNotPresent
          args:
            - "-namespace"
            - "default"
            - "-reconcile-interval"
            - "30s"
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
